package com.vrozsa.crowframework.screen.ui.api;

public interface UIFillBar {
    /**
     * Set slide bar fill (must be between fill >= 0 and fill <= 1);
     *
     * @param fill
     */
    void setFill(float fill);
}
